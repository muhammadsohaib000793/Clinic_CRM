# Railway/production builds set NODE_ENV=production, which makes `npm ci` omit
# devDependencies. But the frontend build tool (vite) is a devDependency, so the
# build would fail with "vite: not found". Force dev deps to install at build time.
include=dev
